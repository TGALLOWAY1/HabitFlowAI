# HabitFlow Docs

This `docs/` folder is the canonical documentation home for HabitFlow.

Start here:
- `docs/DOC_INDEX.md` for the full map.

Reference sources are preserved in:
- `docs/reference/` (canonical vocabulary/spec documents)
- `docs/archive/` (historical material)

For code-level conventions, also see:
- `src/server/lib/README.md`
- `src/server/repositories/README.md`
