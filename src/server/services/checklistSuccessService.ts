/**
 * Checklist Success Service
 *
 * Re-exports the shared evaluateChecklistSuccess function.
 * Backend code should import from here to maintain the existing import paths.
 */

export { evaluateChecklistSuccess } from '../../shared/checklistSuccessRule';
export type { ChecklistSuccessRule } from '../../shared/checklistSuccessRule';
